graph = [
    [],
    [(2, 2), (3, 5), (4, 1)],  # 1
    [(3, 3), (4, 2)],  # 2
    [(2, 3), (6, 5)],  # 3
    [(3, 3), (5, 1)],  # 4
    [(3, 1), (6, 2)],  # 5
    []
]

q = [(0, 1)]

distance = [
    [],
    [0],
    [INF],
    [INF],
    [INF],
    [INF],
]

dist = 0  # 출발 노드에서 현재 노드까지의 거리
now = 1  # 현재 위치한 노드
cost = dist + graph[now[1]]
